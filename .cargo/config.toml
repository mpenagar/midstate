[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
rustflags = ["-C", "target-cpu=cortex-a53", "-C", "link-arg=-lgcc"]

[build]
# Optimise for size + speed on the Pi
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
